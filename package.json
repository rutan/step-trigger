{
  "name": "@rutan/step-trigger",
  "version": "0.1.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "author": "ru_shalm <ru_shalm@hazimu.com>",
  "license": "MIT",
  "homepage": "https://github.com/rutan/step-trigger",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rutan/step-trigger"
  },
  "files": [
    "src",
    "lib"
  ],
  "scripts": {
    "build": "npm run lint && npm run test && npm run clean && run-s build:*",
    "build:ts": "NODE_ENV=production tsc",
    "clean": "rimraf ./lib",
    "watch": "tsc -w",
    "test": "mocha --compilers ts:espower-typescript/guess test/**/*.ts",
    "lint": "tslint -c tslint.json ./src/**/*.ts",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.40",
    "espower-typescript": "^8.0.0",
    "mocha": "^3.2.0",
    "npm-run-all": "^4.0.2",
    "power-assert": "^1.4.2",
    "rimraf": "^2.6.1",
    "tslint": "^5.0.0",
    "typescript": "^2.2.2"
  },
  "private": true
}
